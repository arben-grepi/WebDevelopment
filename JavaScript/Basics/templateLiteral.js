const message = "This is my\n" + "'first' message";

console.log(message);

// with backtick we can log our message in the way we want
const anotherMessage = `This is my 
'second' message`;

console.log(anotherMessage);

const ArbeninNimi = "arben";
const thirdMessage = `Moro ${ArbeninNimi}`;

console.log(thirdMessage);
